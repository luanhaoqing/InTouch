# INSTANT STATE

case 1:
    Controller.ShowUp();
    substate = 2;
    break;

# WAITING STATE

case 4:
    if (Controller.hasDesiredInput("Right")) {
        substate = 5;
    }
    break;

# CONTINUOUS STATE

case 5:
    Cursor.MoveRight()
    counter = 0
    substate = 5.1
    break;
case 5.1:
    if (counter < 3) {
        counter = counter + Time.deltaTime;
    } else {
        substate = 6
    }
    break;